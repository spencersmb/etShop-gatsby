import { IProducts } from '@et/types/Products'

export interface IState {
  count: number,
  products: IProducts
  // breakPoint: number,
  // cart: ICartState,
  // modal: IModalState,
  // products: IProductState,
  // user: IUserState
}