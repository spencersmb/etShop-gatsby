export interface IGatsbyConfig {
	site: {
		siteMetadata: {
			title: string,
			description: string,
			author: string,
			authorUrl: string,
			siteUrl: string,
			siteName: string,
			db: string,
			route: string,
			twitterUrl: string
		},
	}
}